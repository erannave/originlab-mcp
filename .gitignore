# Runtime artifacts written by the sidecar / manager
host.handshake
server.lock
sidecar.log
__pycache__/
*.pyc
vendor/
startup.log
stop.request
# Generated OPX output
*.opx
# Dropped into the app root by the OPX installer (it is packed from packaging/).
# NEVER treat it as a source file here: a package.ini in the root makes Code
# Builder's Generate bundle vendor/ + .git. See "Building the OPX" in CLAUDE.md.
package.ini
